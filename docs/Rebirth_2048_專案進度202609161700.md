# Rebirth 2048 — 專案進度（Canonical Project Progress）

最後更新：2026-09-16 16:55（台灣時間）
規則依據：`Rebirth_2048_專案規則202609161640.md`
專案：`johnbutyno3/evolution_2048`
分支：`feature/chapter1-spec-implementation`

> 本文件完整延續 Git 歷史中的 `Rebirth_2048_專案進度202609161645.md`，保留上一版本全部內容，不以摘要取代，並在文件末端追加本次 54 個獨立 Avatar catalog 與 Home／Player Info index mapping 修正紀錄。後續版本必須以本完整版本繼續累積。

---

# 1. 目前階段

**Personal Information + Shop / Marketplace → Security / Anti-Cheat / Server Authority hardening**

目前重點不是重做已完成的 Membership、Gold 或 Tool Inventory foundation，而是完成 Personal／Shop UI 驗證、Game Session / Replay security，以及 server-side security enforcement。

---

# 2. 最新 GitHub 狀態

目前 branch 最新 committed state 已進入本次 54-avatar catalog mapping 修正鏈。

本次新增：
- `4eed3d6` — `security: enforce sensitive operations server-side`
- `2a48bc9` — `docs: extend canonical rules with segmented large-file workflow`
- `3b030b9` — `fix: align home avatar index mapping with 54-avatar catalog`
- 本進度文件 — `docs/Rebirth_2048_專案進度202609161655.md`

`functions/index.js` 原始 blob：`a9d7198feb9a404e9f3158bf77aec9686e52ee23`。
Security Enforcement 修改後 blob：`c58aa590690a3631c6e8d166aab4db5202a6518f`。

近期 security foundation：
- Game Session / Replay foundation。
- Centralized Security Enforcement。
- Cumulative Risk Score。
- Temporary Critical Restriction。
- Security Enforcement Boundary Tests。
- Security Audit integration。

---

# 3. 文件來源與版本管理

File Library 目前仍受寫入／覆蓋限制。

Git fallback canonical rules 最新：
`Rebirth_2048_專案規則202609161640.md`

Git fallback canonical progress 最新：
`Rebirth_2048_專案進度202609161655.md`

本文件不是短摘要，而是延續 16:45 完整版本後再追加最新資訊。後續任何進度版本都必須從本文件或其後最新完整版本延續，不得回退至短版。

待 File Library 恢復寫入能力後：
1. 將本進度新增至 `/2048/`。
2. 將最新完整規則新增至 `/2048/`。
3. 不覆蓋舊檔、不要求專案經理手動搬移。
4. 以最新時間戳完整版本作為有效版本。

---

# 4. Server-authoritative Gold

## 已完成
- [x] Gold read / spending server foundation。
- [x] Wallet client write protection foundation。
- [x] Client Gold grant/reset APIs removed。
- [x] No callable client Gold grant。
- [x] Server-only `grantGoldToUser()` helper。
- [x] Firestore transaction。
- [x] Gold amount/source validation。
- [x] Lifetime granted/spent accounting protection。

## 尚未完成
- [ ] Verified-payment Gold grant。
- [ ] Receipt verification。
- [ ] Purchase transaction idempotency ledger 完整串接。

正式付款發獎不得在 receipt verification 前假裝完成。

---

# 5. Membership

## 規則
- FREE：免費。
- PREMIUM：USD 2.99/month。
- GOLDEN：USD 5.99/month。
- PREMIUM：No Ads + Life 30-minute recovery。
- GOLDEN：No Ads + Infinite Lives。
- GOLDEN UNDO：∞。

## 已完成
- [x] Membership server read foundation。
- [x] Golden Infinite Lives derived from server membership foundation。
- [x] Membership callable architecture。
- [x] 集中化 `resolveMembership()`。
- [x] `getMembershipStatus`、`purchaseTool`、`useTool`、`startGameSession` 使用同一 Membership resolver。
- [x] `node --check functions/index.js` 通過（歷史驗證）。
- [x] `flutter analyze` 通過（歷史驗證）。
- [x] `git diff --check` 通過（歷史驗證）。
- [x] Membership centralization commit：`eb88ca8`。

## 尚未完成
- [ ] Store receipt verification。
- [ ] Subscription validity / expiration verification。
- [ ] Verified purchase → Membership grant。
- [ ] Client modified membership 的完整拒絕驗證。
- [ ] GOLDEN UI 最終 Chrome 實測。

---

# 6. 核心遊戲架構

- [x] 4×4 board。
- [x] 2048 核心移動／合成架構。
- [x] 六章權威架構。
- [x] 舊 18 階 creature system 不再作為現行規則。
- [x] C1–C6 tool permission 架構。
- [x] 工具庫存永久累積模型。
- [x] Chapter lock 與 inventory 分離。

現行六章：
- C1 Ocean：12 stages，最高 4096。
- C2 Land：13。
- C3 Sky：14。
- C4 History：15。
- C5 Technology：16。
- C6 Space / Universe：17，final。

不得恢復舊版 2048 解鎖、4096 額外挑戰或 18-tier 系統。

---

# 7. Personal Information

## 已建立
- [x] Personal Page。
- [x] Player Info 架構。
- [x] Evolution Progress。
- [x] 永久 Creature Collection entry。
- [x] Settings / Game Guide / Version / Logout entry。
- [x] Player profile uniqueness architecture。
- [x] Secure player profile indexes。

## 待驗證／待完成
- [ ] Avatar persistence。
- [ ] Player name persistence。
- [ ] Authenticated Firebase persistence。
- [ ] Re-login 後恢復 Avatar / Player Name。
- [ ] Personal Page avatar entry / layout final verification。
- [ ] 移除 PLAYER INFO 額外 menu item。
- [ ] 移除 Personal Information 中獨立 Gold menu item。
- [ ] Language selection final verification。
- [ ] GAME DATA 完全移除並確認不可恢復。
- [ ] Detailed Game Guide。
- [ ] Actual app version display。
- [ ] Dedicated Game Information / Credits。
- [ ] Asset / music / third-party attribution verification。

---

# 8. Home

## 已建立
- [x] Home → Personal。
- [x] Home → Shop。
- [x] Avatar asset list 接入。
- [x] Home 讀取目前 Avatar。
- [x] Home Avatar index order 已與 Player Info 54-avatar catalog 統一。

## 待驗證
- [ ] Home 六章畫面正常載入與顯示。
- [ ] C1 初始解鎖。
- [ ] 其他章節依 server-authoritative progression 解鎖。
- [ ] 不恢復舊 18 階畫面。

---

# 9. Creature Collection

## 已建立
- [x] Firebase account-scoped collection data layer。
- [x] `users/{uid}/progress/collection`。
- [x] Collection 與 mutable game save 分離。
- [x] 永久 additive data architecture。
- [x] Server callable `discoverCreature`。
- [x] Firestore collection progress client writes locked to admin/server。

## 待完成
- [ ] 實際 gameplay evolution event 寫入 Collection。
- [ ] 新 creature/stage 達成後自動保存。
- [ ] Collection Page 正常載入／顯示。
- [ ] relogin 後確認資料仍存在。
- [ ] replay / Game Over 後確認已發現物件不消失。
- [ ] 不建立第二套 progress system。

---

# 10. Shop / Marketplace

## 已建立
- [x] Shop Page。
- [x] Gold 購買概念。
- [x] Tool purchase / owned quantity 架構。
- [x] Remote Shop Config / `shop_config/global`。
- [x] Purchase intent architecture。
- [x] Transaction uniqueness protection foundation。
- [x] Life 不作獨立商品。
- [x] Gold authenticated read foundation。
- [x] Atomic Gold spending foundation。
- [x] Protected wallet write foundation。
- [x] Client Gold grant/reset APIs removed。
- [x] Server-only Gold grant helper。
- [x] Server-side tool-use validation foundation。
- [x] Server-side purchase validation foundation。
- [x] GOLDEN 不可購買 UNDO。
- [x] GOLDEN UNDO 不消耗有限庫存。
- [x] C1–C6 tool permission enforcement。
- [x] Server-authoritative Tool Inventory grants/deductions foundation：`getToolInventory`、`purchaseTool`、`useTool`、chapter reward inventory grant。
- [x] Security Enforcement gate 接入 tool purchase / tool use。

## 待完成
- [ ] Verified-payment Gold grants。
- [ ] Verified-payment Membership grants。
- [ ] Tool Inventory Flutter client 最終整合／Chrome 實測。
- [ ] Legacy local Gold migration policy / implementation。
- [ ] Final Shop UI icon presentation。
- [ ] Final Gold / Tool purchase UI verification。
- [ ] 真實 Google Play receipt verification。
- [ ] 真實 Apple receipt verification。

---

# 11. Life

規則：FREE 5 / 60 min、PREMIUM 5 / 30 min、GOLDEN ∞。Life 不單獨販售。

## 待驗證／整合
- [ ] Final Life entitlement authority。
- [ ] Home → Game 進入時生命扣除。
- [ ] Restart / Game Over / abnormal exit / chapter transition 行為完整驗證。
- [ ] Golden Infinite Lives 完整 UI / flow 驗證。

---

# 12. Tools / Golden UNDO

## 已建立
- [x] 四種工具 server validation。
- [x] Tool Inventory server persistence foundation。
- [x] Chapter permission enforcement。
- [x] GOLDEN UNDO = ∞。
- [x] GOLDEN 不購買 UNDO。
- [x] GameEngine 四種工具操作走 server `useTool()`。
- [x] ToolManager server inventory/use/purchase path。
- [x] `purchaseTool` / `useTool` sensitive-operation enforcement gate。

## 待驗證
- [ ] Tool Inventory Flutter final integration / Chrome。
- [ ] GOLDEN UNDO UI 顯示 `∞`。
- [ ] GOLDEN UNDO purchase button hidden。
- [ ] Rewarded Ad UNDO 每局最多 2 次。
- [ ] Tool selection sound `tool_select.mp3`。

---

# 13. Replay / Anti-Cheat

## 已建立 foundation
- [x] Game session security architecture。
- [x] Replay validation 已包含 chapter tool permission。
- [x] Session ownership / chapter range / target validation architecture。
- [x] Suspicious request logging architecture。
- [x] Flutter `Evolution2048Page` active Game Session integration。
- [x] Game session started after gameplay resume when needed。
- [x] Restart clears and recreates the active session。
- [x] Tool start is gated by active session availability。
- [x] Chapter completion reuses active session and submits `replayLog`。
- [x] Chapter completion only proceeds when server completion callable accepts the session/replay request。
- [x] `ReplayLog.toJson()` includes `version` and `replayVersion` compatibility fields。
- [x] `ReplayLog.tryFromJson()` accepts either version field and rejects unsupported versions。
- [x] Centralized Security Enforcement module exists。
- [x] Critical risk can create temporary server-side enforcement restriction。
- [x] Security Enforcement boundary tests pass。
- [x] Sensitive callable gate integration completed in `functions/index.js`。

## 尚未完整完成
- [ ] GameEngine 完整連接 ReplayRecorder。
- [ ] 每一次成功普通 move 記錄。
- [ ] 每次 move 的確切 spawn index/value 完整記錄。
- [ ] Tool operation success recording。
- [ ] 精確 UNDO replay semantics。
- [ ] Active-board save/store replay restore。
- [ ] 完整 server deterministic replay validation。
- [ ] 不再信任 client final score / highest value。
- [ ] Final authoritative completion path deploy/test。
- [ ] Firebase deployed callable functional verification。

因此目前仍不得宣稱完整 Replay Anti-Cheat 已完成；本次只是完成 server-side enforcement gate integration。

---

# 14. Game Session

## 已建立
- [x] `startGameSession({chapterIndex})`。
- [x] `useTool({sessionId, toolType})` session validation architecture。
- [x] `completeChapter({sessionId, chapterIndex, replayLog})` architecture。
- [x] Session ownership。
- [x] One-time completion。
- [x] Chapter range validation。
- [x] Target validation。
- [x] Suspicious request logging。
- [x] Active game `sessionId` integration in Flutter evolution screen。
- [x] Chapter completion reuses the active session。
- [x] Restart/session reset path integrated。
- [x] Game Over path clears the active session before leaving the game。
- [x] Tool gate ensures a session exists before starting a tool。
- [x] Engine completion path submits the generated replay log through `completeChapter()`。
- [x] `startGameSession` now checks Security Enforcement before sensitive session creation。
- [x] `completeChapter` now checks Security Enforcement before sensitive completion processing。

## 本機已知驗證紀錄
- [x] 先前 `git pull origin feature/chapter1-spec-implementation` fast-forward 至 `f81433f`。
- [x] 當時 `flutter analyze` → `No issues found!`。
- [x] 當時 `git diff --check` → passed with no output。
- [x] 當時 `node --check functions/index.js` → passed。
- [x] 當時 `node --check functions/profile.js` → passed。
- [x] 當時 `git status` → branch up to date with origin，working tree clean。

注意：以上是歷史本機驗證，不代表目前本機已同步到最新 GitHub state。

## 待完成
- [ ] GameEngine 完整 replay event recording。
- [ ] 每次 move 的確切 spawn index/value 完整記錄。
- [ ] Tool operation success recording。
- [ ] 精確 UNDO replay semantics。
- [ ] Active-board save/store replay restore。
- [ ] Server deterministic replay 最終驗證。
- [ ] Final completion 不依賴 client score/highestValue。

---

# 15. Firestore / Server Authority

## 基礎已建立
- [x] Gold read / spending server foundation。
- [x] Wallet client write protection foundation。
- [x] Membership server read foundation。
- [x] Tool inventory / use validation foundation。
- [x] Chapter progression server authority。
- [x] Game session authority。
- [x] Security event architecture。
- [x] Purchase intent / transaction uniqueness foundation。
- [x] Server-only Gold grant helper。
- [x] Collection discovery server callable。
- [x] Centralized Security Enforcement module。
- [x] Sensitive callable enforcement gates wired in `functions/index.js`。

## 待完成
- [ ] Tighten `users/{uid}` update permissions。
- [ ] Verified resource grants。
- [ ] Final Membership authority。
- [ ] Final Tool Inventory authority。
- [ ] Final Life entitlement authority。
- [ ] Full receipt verification。
- [ ] Full anti-cheat audit/admin enforcement。
- [ ] Production deployment and callable integration verification。

---

# 16. Settings / Guide / Version / Credits

## 已建立
- [x] Settings entry。
- [x] Game Guide entry。
- [x] Version Info entry。
- [x] Logout entry。

## 待完成
- [ ] Language selection final verification。
- [ ] GAME DATA 完全移除。
- [ ] Music / SFX settings final verification。
- [ ] Detailed Game Guide。
- [ ] Actual app version display。
- [ ] Dedicated Game Information / Credits。
- [ ] Asset / music / third-party attribution verification。

---

# 17. UI Functional Findings

以下不得標示完成，直到實際 Chrome／Firebase 測試通過：
1. Personal Page avatar entry/layout。
2. Avatar persistence。
3. Player name persistence。
4. Firebase authenticated persistence。
5. Home six-chapter display。
6. Creature Collection display。
7. Collection gameplay event integration。
8. Remove separate Gold menu from Personal Information。
9. Settings language verification。
10. GAME DATA complete removal。
11. Detailed Game Guide。
12. Actual Version Information。
13. Game Information / Credits。
14. Shop Gold / Tool icon presentation。
15. GOLDEN UNDO UI `∞`。
16. GOLDEN UNDO purchase button hidden。
17. Rewarded Ad UNDO 2-times-per-game behavior。
18. Active Game Session / Replay integration final Chrome/Firebase functional verification。
19. Security Enforcement deployed callable functional verification。

---

# 18. Payment Integration

暫緩：Google Play real payment verification、Apple App Store real payment verification、Store receipt verification、Subscription state synchronization。

原因：必須先完成 Google Play Console 與 Apple Developer / App Store Connect 正式申請及付款商品設定；不得以假付款成功替代正式流程。

---

# 19. 明確下一步

依目前實際 Git committed state，不重做已完成 foundation。

1. **確認 Personal Information / profile 最終整合。**
2. **確認 Shop / Tools UI 與 server inventory 一致。**
3. **整合 Game flow 的 tool / life / membership / server authority。**
4. **完成 GameEngine replay event recording 與 deterministic replay validation。**
5. **收緊 `users/{uid}` update permissions。**
6. **隔離 production developer unlimited/reset capabilities。**
7. **部署並驗證 Security Enforcement integration。**
8. **完成 Creature Collection gameplay event integration / Collection Page verification。**
9. 每個明確工作段落完成後：實際驗證 → `flutter analyze` → `git diff --check` → commit → 更新完整進度 → 再進下一項。

---

# 20. 文件管理與恢復

規則／進度變更時建立新 timestamped file，不覆蓋舊檔。新對話必須比較 File Library `/2048/` 與 Git `docs/` 最新日期時間版本，再讀規則、進度、GitHub committed state 與實際程式碼。不得恢復舊 18-tier、舊 l10n、舊 backup 或衝突歷史。

目前 Git canonical 文件原則：最新 timestamped rules 與最新 timestamped progress 為工作基準；歷史版本如已明確判定 obsolete，刪除後不得重新作為現行來源。若舊檔仍保留作歷史，仍不得以較舊 timestamp 取代最新版本。

---

# 21. 本次完整更新紀錄

### A. Game Session / Replay 整合（承接 14:30）
- [x] `Evolution2048Page` 建立 `_gameSessionStarting` 狀態，避免重複 start request。
- [x] `_ensureGameSession()` 在需要時啟動 session，並等待正在進行的 session start。
- [x] `initState` 在恢復未完成遊戲後建立 session。
- [x] Restart 清除舊 session 並建立新 session。
- [x] Tool flow 在進入工具前確認 active session。
- [x] Game Over 離開前清除 session。
- [x] Chapter Complete 重用 active session，提交 replayLog。
- [x] Server completion 回傳拒絕時不進入 completion screen。
- [x] ReplayLog serialization/parser version compatibility 已整合。

### B. One-time workflow cleanup（承接 14:30）
- [x] 移除 `.github/workflows/security-audit-migration.yml` 的一次性 Game Session / Replay patch step。
- [x] 移除 workflow 對一次性 patch 檔案的 staging/deletion 依賴。
- [x] Workflow 不再因不存在的一次性檔案而在 commit step 失敗。
- [x] 相關程式碼已直接提交到正式 branch，不再依賴 one-time Actions patch。

### C. Security Enforcement Layer
- [x] 建立集中式 `functions/security_enforcement.js`。
- [x] 建立 `enforceSensitiveOperation()` server gate。
- [x] Sensitive operation 類型涵蓋 `purchase_tool`、`use_tool`、`spend_gold`、`start_game_session`、`complete_chapter`、`purchase`、`grant_membership`。
- [x] Enforcement 依累積 security risk score 判定 NORMAL / WARNING / ADMIN_ALERT / CRITICAL。
- [x] NORMAL：允許。
- [x] WARNING：允許但保留風險狀態。
- [x] ADMIN_ALERT：允許操作但進入監控／管理警示層。
- [x] CRITICAL：拒絕 sensitive operation。
- [x] 已存在有效 temporary restriction 時拒絕 sensitive operation。
- [x] 過期 restriction 可恢復正常允許狀態。
- [x] `applyCriticalRestriction()` 建立 temporary restriction，預設 24 小時、最長 30 天。
- [x] `security_enforcement/{uid}` enforcement state 僅允許 server 寫入。
- [x] client 無法直接修改 enforcement state。
- [x] Security enforcement boundary tests 已實際執行並通過。

### D. Critical Risk → Temporary Enforcement
- [x] `security_audit` 的 CRITICAL risk 路徑已接到 temporary enforcement。
- [x] critical security risk 可呼叫 `applyCriticalRestriction()`。
- [x] restriction duration 有上限，不允許由外部輸入無限制延長。
- [x] restriction state 寫入 server-controlled `security_enforcement/{uid}`。

### E. Security Enforcement Boundary Tests
- [x] 新增 `functions/security_enforcement.test.js`。
- [x] 覆蓋 NORMAL allow boundary。
- [x] 覆蓋 WARNING allow boundary。
- [x] 覆蓋 ADMIN_ALERT allow/monitor boundary。
- [x] 覆蓋 CRITICAL deny boundary。
- [x] 覆蓋 stored CRITICAL deny。
- [x] 覆蓋 active temporary restriction deny。
- [x] 覆蓋 expired restriction recovery。
- [x] 覆蓋 invalid operation rejection。
- [x] 覆蓋 `applyCriticalRestriction()` 最大 30 天限制。
- [x] 覆蓋 invalid uid rejection。
- [x] 實際執行 `node security_enforcement.test.js`：Security enforcement boundary tests passed。
- [x] 測試不是只檢查檔案存在，而是實際執行 boundary assertions。

### F. Functions dependency validation
- [x] 在 `functions` 執行 `npm install`。
- [x] 結果：`up to date, audited 243 packages in 1s`。
- [x] npm 回報 8 moderate severity vulnerabilities；目前不執行 `npm audit fix` 或 `npm audit fix --force`，避免無關 dependency 變更干擾 security 工作。
- [x] `node security_enforcement.test.js` 實際通過。

### G. Canonical document cleanup
- [x] 已清理重複 canonical rules / progress 文件。
- [x] 舊重複 progress/rules timestamp 文件不再作為現行 source of truth。
- [x] `Rebirth_2048_專案進度202609161600.md` 被確認為錯誤縮短版，不得作為後續母檔。
- [x] 本次重新建立完整 progress：`Rebirth_2048_專案進度202609161547.md`。
- [x] 本版本以 Git 歷史中的 14:30 完整版為母檔，再追加 14:30 後已確認資訊。

### H. 不可遺失的進度版本規則強化
- [x] 新進度版本不得比上一個真正完整版本更短。
- [x] 新進度必須保留上一個完整版本的所有重要章節、完成／未完成狀態與歷史驗證紀錄，再追加新工作。
- [x] 不得以短摘要重建 canonical progress。
- [x] 若發現新版本縮短，應停止採用該版本並從最後真正完整版本重建。
- [x] canonical progress 的時間戳必須更新，內容必須可追溯。

### I. 大型 GitHub 檔案分段讀取實作
- [x] 確認大型 GitHub 檔案單次輸出截斷不代表檔案無法處理。
- [x] 先取得 `functions/index.js` 實際 blob SHA `a9d7198feb9a404e9f3158bf77aec9686e52ee23`。
- [x] 以同一 branch/blob 版本分段讀取來源內容。
- [x] 完整取得後才建立修改版本，避免以 partial/truncated content 覆蓋大型檔案。
- [x] `functions/index.js` 本次只加入 Security Enforcement import 與 8 個 sensitive callable gates。
- [x] GitHub commit diff 已核對，確認沒有大範圍重寫或截斷覆蓋。

### J. Security Enforcement Callable Gate Integration
- [x] `functions/index.js` 加入 `const { enforceSensitiveOperation } = require('./security_enforcement');`。
- [x] `purchaseTool` → `purchase_tool` gate。
- [x] `useTool` → `use_tool` gate。
- [x] `grantMembership` → `grant_membership` gate。
- [x] `spendGold` → `spend_gold` gate。
- [x] `createPurchaseIntent` → `purchase` gate。
- [x] `submitPurchaseForVerification` → `purchase` gate。
- [x] `startGameSession` → `start_game_session` gate。
- [x] `completeChapter` → `complete_chapter` gate。
- [x] Gate 均位於 authentication check 後、敏感處理前。
- [x] Git commit `4eed3d65d89db9fb43028513dfd052a6832995d4` 實際 diff 逐項確認 1 import + 8 gates。

---

# 22. 不可遺失的工作原則

- 本專案不是以「短摘要」保存進度。
- 新版本必須延續最新完整規則／進度，再加入新資訊。
- Git fallback 不得縮短 canonical 文件。
- 檔案庫恢復寫入後必須新增完整版本至 `/2048/`。
- 不要求專案經理手動搬移、複製或整理文件，除非工具權限確實不足且沒有其他可行方式。
- 大型檔案單次讀取若截斷，必須分段讀取；未取得完整內容前不得覆蓋原始大型檔案。
- 本機目前可能仍保留先前錯誤的未提交修改；未經實際本機檢查不得把它視為 GitHub canonical state。
- 本次 GPT 已先直接修正 GitHub，沒有要求專案經理用 PowerShell 修理 GPT 自己造成的修改錯誤。

---

# 23. 54 個獨立 Avatar catalog 修正（2026-09-16 16:55）

## 已確認的正式資產
- [x] `assets/avatars/` 目前存在 **54 張獨立 PNG Avatar 圖片**。
- [x] 組成為三組各 18 張：Original / White、Black / African、Asian。
- [x] 每組均包含 Ancient / Modern / Future × Young / Middle / Elder × Male / Female。
- [x] `pubspec.yaml` 已以 `assets/avatars/` 整個資料夾納入資產。
- [x] GitHub 目前未找到 `avatar_sheet.png` 引用。
- [x] Git tree 中未找到 `assets/avatars/avatar_sheet.png`；目前正式資產不是 sprite sheet。

## 發現的實際問題
- [x] `PlayerInfoPage` 已使用 54 個獨立 Avatar 路徑，並將 `avatarIndex` 限制在 `0..53`。
- [x] `PlayerProfileService` 的 `updateAvatarIndex()`、`ensureProfile()` 與 profile 更新流程同樣使用 `0..53`。
- [x] 但是 `HomePage` 原本的 `_homeAvatarAssets` 排序與 `PlayerInfoPage` 不一致。
- [x] 因此同一個 Firebase `avatarIndex` 在 Personal／Player Info 與 Home 可能顯示不同人物。

## 已修正
- [x] Home 的 54 個 Avatar index order 已改為與 Player Info 完全相同。
- [x] canonical order：
  - Original / White：18
  - Black / African：18
  - Asian：18
- [x] 每組順序統一為：Ancient young male/female → Ancient middle male/female → Ancient elder male/female → Modern young male/female → Modern middle male/female → Modern elder male/female → Future young male/female → Future middle male/female → Future elder male/female。
- [x] 修正 commit：`3b030b90b23e697aa7615de60a2b910feb026ac2`。
- [x] 這次修正沒有恢復或建立任何 Avatar Sheet。

## 正式結論
**Rebirth 2048 現行 Avatar 方案 = 54 張獨立圖片；`avatar_sheet.png` 不屬於現行方案。**

後續不得重新建立 sprite sheet、12 格 Avatar mapping 或其他與 54 個獨立 Avatar catalog 衝突的索引方案。

---

# 24. Personal Information／Home Avatar 導航架構修正（2026-09-16 17:00）

## 發現
- [x] 實際檢查 `lib/screens/home_page.dart` 與 `lib/screens/personal_page.dart`。
- [x] Home Avatar 原本透過 `_openPlayerInfo()` 導向獨立 `PlayerInfoPage`。
- [x] 這與目前產品架構「Avatar 直接進 Personal、取消獨立 PLAYER INFO」不一致。
- [x] `PersonalPage` 本身目前仍是功能入口頁；原本的 Avatar／Player Name 編輯 UI 位於獨立 `PlayerInfoPage`。

## 已修正
- [x] `HomePage._openPlayerInfo()` 已改名為 `_openPersonal()`。
- [x] Home Avatar tap 現在直接 `push(const PersonalPage())`。
- [x] 返回 Home 後仍重新執行 `PlayerProfileService.ensureProfile()` 並刷新 Home，避免個人資料狀態停留在舊快取。
- [x] 本次只修改 Home 導航，不恢復舊 Avatar Sheet，也沒有新增第二套 Avatar catalog。
- [x] GitHub commit：`a013ee430ff7ce217c424a2e855e594c6c63c163`。
- [x] 修改前已取得完整 `home_page.dart` blob SHA：`7c5e1ee74cb2b0b3bc252b934995436bf02e4c9c`，並以完整內容更新，沒有使用截斷內容覆蓋大型檔案。

## 尚未標示完成
- [ ] PersonalPage 內直接整合 Avatar／Player Name／Player ID／Membership profile 區塊。
- [ ] 移除 `PlayerInfoPage` class 與所有剩餘引用，待 Personal profile UI 整合完成後再做。
- [ ] Personal Page 最終 Chrome UI／Firebase persistence 驗證。

## 下一個明確工作
**將現有 `PlayerInfoPage` 的 54-avatar、Player Name、Player ID、Membership 功能安全整合進 `PersonalPage`，完成後再移除獨立 `PlayerInfoPage`。**
