# Rebirth 2048 — 專案進度（Canonical Project Progress 延續版）

最後更新：2026-09-22 21:45（Asia/Taipei）
規則依據：最新《Rebirth 2048 專案規則》／《Rebirth 2048 鐵律》
專案：`johnbutyno3/evolution_2048`
分支：`feature/chapter1-spec-implementation`

> 本文件為前一份 timestamped canonical progress 的延續同步版。只把已提交到 GitHub 的狀態列入完成；未提交本機修改不列入。既有規則、歷史紀錄與未完成項目不得因本次同步被刪除或誤標完成。

---

# 1. 目前階段

**Personal Information + Shop / Marketplace → Life / Game Session runtime correctness → Launch Security / Anti-Cheat hardening**

---

# 2. 本次先檢查的最新 GitHub HEAD

目前 branch 最新提交為：

- `d96e1c1181f5d1323af2044a56d08848797cb555` — `fix: show authoritative life balance in home and game`
- `fb1ceac226d36231dca03796b33b31392e46fecf` — `fix: apply authoritative restart life state`
- `3c9de6513bef17e7698085f7275542d157fd8588` — `fix: return resume life state from transaction`
- `026a6d4aa8b725a3edd182f2295d564cc184b2ca` — `fix: return transaction-authoritative life state`

近期同一工作段落亦包含：

- `cc75a0135996a1d16220c32c779cf3dfbf45e4d2` — 收斂 `purchaseTool` 至唯一 Tool module。
- `27f65c5b43999ffc929f57926f7c89f10344d9f8` — 移除重複 `restartGameSession` / `abandonGameSession`。
- `8fadddb03aedb7604ef397629cf34fe788932106` — 修正 `getToolInventory` transaction 讀寫順序。
- `8c9497fec6d2ed61ea6098ad6386661a88da731d` — `completeChapter` 讓 replay validation 使用 Test Mode 工具白名單。
- `16a827326810a374deec2c86029bfefd39bd3184` — 移除 `index.js` 重複 Tool Inventory callable。
- `a4bc2859dc5d4acfb546cc21ce990ed67c1c2892` — Replay validator 支援 Test Mode 全工具。
- `21d08c911b9a489413633bfbdfb90e5b0fb4194e` — 修正 bootstrap 實際使用的 Tool module。

---

# 3. 本次已提交且可確認的開發進展

## 3.1 Life authoritative state 回傳與顯示

- [x] `start/resume/restart` 相關 transaction 現在回傳 authoritative life state。
- [x] Home 與 Game UI 已改為顯示 server-authoritative life balance，而不是只依賴舊的本機顯示值。
- [x] Restart 後的生命狀態回傳已補強。
- [x] Resume transaction 回傳生命狀態已補強。
- [x] 生命 transaction 結果與 client refresh 的資料流已進一步對齊。

> 以上代表 Git 程式已完成對齊修正；不等於 Firebase 線上 callable 已完成全量部署與 E2E 驗證。

## 3.2 Callable 單一來源與部署一致性

- [x] Tool Inventory callable 的實際 bootstrap owner 已收斂至 `functions/tool_inventory_defaults.js`。
- [x] `getToolInventory`、`purchaseTool`、`useTool` 不再由多份同名實作競爭。
- [x] Game Session callable 的 `restartGameSession` / `abandonGameSession` 已收斂至 `functions/game_session.js`。
- [x] Test Mode 全工具狀態已同步至 inventory、useTool 與 replay validation。
- [x] 已識別並處理「Git 修正一份、bootstrap 實際執行另一份」的根本風險。

## 3.3 商城工具與測試模式

- [x] Flutter `useTool` 已傳入目前 `sessionId`。
- [x] Test Mode 可對指定測試帳號開啟全部四種工具。
- [x] 正式帳號仍維持章節工具限制。
- [x] 測試模式切換後，遊戲端能刷新工具狀態。

---

# 4. 仍未完成、不得誤標為完成

- [ ] Firebase Functions 全量 deployment 與目前 Git HEAD 對齊。
- [ ] General 會員新局進入 5 → 4 的線上 runtime E2E。
- [ ] Premium 會員生命恢復規則的線上 runtime E2E。
- [ ] GOLDEN Infinite Lives 的線上 runtime E2E。
- [ ] 未完成退出退款、再 resume 再扣 1 Life 的完整 E2E。
- [ ] Restart 新 session／新棋盤／只扣 1 Life 的完整 E2E。
- [ ] Game Over → Restart / Home 的完整 E2E。
- [ ] Chapter Complete refund、active session clear、duplicate completion rejection 的完整 E2E。
- [ ] 商城購買 → inventory → useTool → session toolUsage → replay validation 的完整 E2E。
- [ ] Test Mode 四工具跨章節的完整 E2E。
- [ ] Flutter analyze CI workflow 的實際 run 結果尚未確認；不能把 workflow 建立視為通過。
- [ ] Creature Collection gameplay event、relogin、Game Over、replay 後永久保存驗證仍未完成。
- [ ] 真正 Google Play / Apple receipt verification 依既定規則仍暫緩。

---

# 5. 下一個明確工作項目

1. 先同步目前 GitHub 最新 HEAD 到本機。
2. 執行並確認 `flutter analyze` 通過。
3. 部署目前 Git 的 Firebase Functions（project `rebirth-2048`，region `us-central1`）。
4. 驗證 `getLifeState / start / resume / restart / abandon / complete` 線上 callable 的 authoritative life state。
5. 驗證 General 會員新局是否確實 5 → 4，並確認 Home / Game 顯示一致。
6. 驗證商城工具完整流程與 replay validation。
7. 驗證 Test Mode 開關只影響測試帳號，不改變正式章節規則。
8. 完成上述 runtime 驗證後，再更新下一份 timestamped progress file。

---

# 6. 規則與文件差異

- `docs/PROJECT_STATUS.md` 不存在。
- `docs/GAME_RULES.md` 不存在。
- Branch 實際採用 timestamped progress files；本次延續使用 timestamped canonical progress file，未猜測或重建缺失的舊檔名。
- 本次只同步已提交到 GitHub 的 commit 狀態；未提交本機修改不列入完成。
- 本次未改變正式 Life、Membership、Tool、Chapter 或 Shop 規則。

---

# 7. 本次文件同步紀錄

- 新建立：`docs/Rebirth_2048_專案進度202609222145.md`
- 建立原因：同步最新 HEAD `d96e1c1` 及其前置 Life authoritative state、callable single-source、Tool/Test Mode 修正。
- 未刪除既有 timestamped progress files。
- 未把 Git 程式修正誤寫成 Firebase 線上 E2E 已完成。
