# Rebirth 2048 — 專案進度（Canonical Project Progress 延續版）

最後更新：2026-09-23 21:40（Asia/Taipei）
規則依據：最新《Rebirth 2048 專案規則》／《Rebirth 2048 鐵律》
專案：`johnbutyno3/evolution_2048`
分支：`feature/chapter1-spec-implementation`

> 本文件延續上一份 timestamped canonical progress，保留既有歷史與未完成項目；只把已提交到 GitHub 的狀態列入完成。未提交本機修改不列入完成。

---

# 1. 目前階段

**Personal Information + Shop / Marketplace → Life / Game Session runtime correctness → Launch Security / Anti-Cheat hardening**

---

# 2. 本次先檢查的最新 GitHub HEAD

目前 branch 最新提交為：

- `52eef085af767af0211b8474c2d39872c32843c1` — `fix: refresh home state after game session`
- `643e528f969eee080c3f61b8e2ddd42bdd72c183` — `fix: settle life refund before leaving game`
- `bc2102630f942eda62dbad8c2ed0b06a02c4eaa7` — `fix: preserve unfinished board binding for next charged entry`
- `229aaf52fa13fbcd468920bf9dfe612f7122a707` — `fix: make unfinished exit refund transaction-safe`

近期同一工作段落延續前一版的 Life authoritative state、Callable 單一來源、工具庫存與 Test Mode 修正。

---

# 3. 本次已提交且可確認的開發進展

## 3.1 Home 返回後的 authoritative state refresh

- [x] Home 進入遊戲的 navigation 現在等待遊戲頁返回。
- [x] 遊戲結束／返回 Home 後，Home 會重新 refresh chapter progress、server-authoritative Life balance 與 Tool inventory。
- [x] Home header 不再固定顯示進入遊戲前的舊生命數值。

## 3.2 生命退款與未完成局狀態

- [x] 離開未完成遊戲前先 settle life refund，再離開遊戲頁。
- [x] 未完成棋盤的 binding 會保留到下一次應扣生命的進入流程，避免狀態遺失。
- [x] 未完成退出退款改為 transaction-safe，避免重複退款或狀態競爭。

> 以上是已提交的程式修正；仍不等於 Firebase Functions 已部署並完成線上 E2E。

## 3.3 前一版已確認的基礎

- [x] `start/resume/restart` transaction 回傳 authoritative life state。
- [x] Home 與 Game UI 顯示 server-authoritative life balance。
- [x] Tool Inventory callable 與 Game Session callable 已收斂至單一來源。
- [x] Flutter `useTool` 已綁定目前 `sessionId`。
- [x] Test Mode 工具白名單已同步至 inventory、useTool 與 replay validation。
- [x] 正式帳號仍維持章節工具限制。

---

# 4. 仍未完成、不得誤標為完成

- [ ] Firebase Functions 全量 deployment 與目前 Git HEAD 對齊。
- [ ] General 會員新局進入 5 → 4 的線上 runtime E2E。
- [ ] Premium 會員生命恢復規則的線上 runtime E2E。
- [ ] GOLDEN Infinite Lives 的線上 runtime E2E。
- [ ] 未完成退出退款、再 resume 再扣 1 Life 的完整 E2E。
- [ ] Restart 新 session／新棋盤／只扣 1 Life 的完整 E2E。
- [ ] Game Over → Restart / Home 的完整 E2E。
- [ ] Chapter Complete refund、active session clear、duplicate completion rejection 的完整 E2E。
- [ ] 商城購買 → inventory → useTool → session toolUsage → replay validation 的完整 E2E。
- [ ] Test Mode 四工具跨章節的完整 E2E。
- [ ] Flutter analyze CI workflow 的實際 run 結果尚未確認。
- [ ] Creature Collection gameplay event、relogin、Game Over、replay 後永久保存驗證仍未完成。
- [ ] 真正 Google Play / Apple receipt verification 依既定規則仍暫緩。

---

# 5. 下一個明確工作項目

1. 同步目前 GitHub 最新 HEAD 到本機。
2. 執行並確認 `flutter analyze` 通過。
3. 部署目前 Git 的 Firebase Functions（project `rebirth-2048`，region `us-central1`）。
4. 驗證 `getLifeState / start / resume / restart / abandon / complete` 線上 callable 的 authoritative life state。
5. 驗證 `Game Over → Back → Home → 再進 C1` 是否只按規則扣除一次 Life。
6. 驗證未完成局 resume、refund、下一次 charged entry 的完整流程。
7. 驗證 Home 返回後 Life／Tool／chapter progress 顯示與 server 狀態一致。
8. 完成上述 runtime 驗證後，再更新下一份 timestamped progress file。

---

# 6. 規則與文件差異

- `docs/PROJECT_STATUS.md` 不存在。
- `docs/GAME_RULES.md` 不存在。
- Branch 實際採用 timestamped progress files；本次延續使用 timestamped canonical progress file，未猜測或重建缺失的舊檔名。
- 本次只同步已提交到 GitHub 的 commit 狀態；未提交本機修改不列入完成。
- 本次未改變正式 Life、Membership、Tool、Chapter 或 Shop 規則。

---

# 7. 本次文件同步紀錄

- 新建立：`docs/Rebirth_2048_專案進度202609232140.md`
- 建立原因：同步最新 HEAD `52eef085` 及其 Life refund、unfinished board binding、Home refresh 修正。
- 未刪除既有 timestamped progress files。
- 未把 Git 程式修正誤寫成 Firebase 線上 E2E 已完成。
